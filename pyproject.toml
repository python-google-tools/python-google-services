[project]
name = "googlepy"
version = "0.1.0"
description = "Google API Python module"
requires-python = ">=3.9"
dependencies = [
    "gspread",
    "google-auth",
    "google-auth-oauthlib",
    "python-dotenv",
    "tomli; python_version < '3.11'",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["googlepy"]

[tool.hatch.build.targets.wheel.sources]
"" = ""
